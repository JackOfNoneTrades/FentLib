Restored vanilla servers.dat behavior (solely adding custom entries, not outright replacing it with a json).
Animated server icons are now stored in separate files.
Files which are not referenced by the servers.dat are cleaned up on preinit.
